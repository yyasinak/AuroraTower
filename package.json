{
  "name": "aurora-tower",
  "version": "1.0.0",
  "private": true,
  "description": "Offline canvas climbing game",
  "scripts": {
    "test": "node --check game.js && node verify.cjs"
  },
  "engines": { "node": ">=22" }
}
